<%= current_user.username if current_user.present? %>

<div class="container-fluid border-util border-radius-util border-radius-shadow-util index-content" ng-controller="AppCtrl">
  <div class="row-fluid content-wrapper">
    <div class="span3 left-main-nav">
    {{emittedMessage}} {{userEmittedMessage}}
      <ul class="main-nav-item">
        <li>
          <a href="{{homeLinkUrl}}">Home</a>
        </li>

        <li>
          <a href="#/test">test page</a>
        </li>
        <li>
          <a href="#/{{userLinkTo}}">{{userLinkTo}}</a>
        </li>
        <li>
          <a href="" ng-controller="ChildLogout" ng-click="onButtonClick()">Child</a>
        </li>
      </ul>

    </div>

    <div class="span7 my-ng-view-content">
      <ng-view></ng-view>
    </div>

  </div>

  <hello-world></hello-world>
</div>

<%#= link_to 'New User', new_user_path %>
